[project]
name = "cell-analyzer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sohel ali", email = "sasalisayyed@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
cell-analyzer = "cell_analyzer:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
